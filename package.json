{
	"name": "nodejs-websocket",
	"version": "1.4.1",
	"author": "Sitegui <sitegui@sitegui.com.br>",
	"description": "Basic server&client approach to websocket (text and binary frames)",
	"main": "./index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/sitegui/nodejs-websocket"
	},
	"keywords": [
		"websocket",
		"websocket-server",
		"websocket-client"
	],
	"license": "MIT",
	"engines": {
		"node": ">=0.10"
	},
	"scripts": {
		"test": "mocha -R spec -b"
	},
	"devDependencies": {
		"mocha": "^2.3.4",
		"should": "^7.1.1",
		"browserify": "*",
		"watchify": "*"
	},
	"scripts": {
	  "build": "browserify -r ./firefoxos.js:bundle -o lib/bundle.js",
	  "watch": "watchify -r ./firefoxos.js:bundle -o lib/bundle.js --debug --verbose",
		"watch-echo": "watchify -r ./firefoxos-echo.js:bundle -o lib/echo.js --debug --verbose"
	}
}
